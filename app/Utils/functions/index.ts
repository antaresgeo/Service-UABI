export const sum = (num1: number, num2: number): number => {
  return num1 + num2;
};

// private decodeJWT() {
//   try {
//     let decode = jwt.verify(this.token, "your-256-bit-secret");
//     console.log(decode);
//     return decode;
//   } catch (error) {
//     console.error(error);
//   }
// }
